### Local environment variables (example)
# Copy this file to `.env.local` and fill in your real keys.
# DO NOT commit `.env.local` — it is ignored by .gitignore

# Claude / Anthropic
VITE_CLAUDE_API_KEY=

# Google Gemini
VITE_GEMINI_API_KEY=

# Ollama (if using a proxy/backend, set to the proxy or omit)
OLLAMA_ENDPOINT=
OLLAMA_MODEL=llama3:latest

# Any other app-specific secrets
# MY_SERVICE_API_KEY=
